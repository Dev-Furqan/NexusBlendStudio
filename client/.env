# Vite client environment variables
# Variables exposed to client code must be prefixed with VITE_

# Base URL for API requests (point to the server). Adjust if server runs on a different host/port.
VITE_API_URL=http://localhost:5000

# App title shown in the UI
VITE_APP_TITLE=Nexus Blend

# Mode hint (not required, Vite sets this automatically but useful to have)
VITE_NODE_ENV=development
